odoo.define('custom_reports.CustomReportsModel', function (require) {
    'use strict';

    var AbstractModel = require('web.AbstractModel');

    var CustomReportsModel = AbstractModel.extend({});

    return CustomReportsModel;
});