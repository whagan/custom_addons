odoo.define('custom_reports.CustomReportsRenderer', function(require) {
    "use strict";

    var AbstractRenderer = require('web.AbstractRenderer');

    var CustomReportsRenderer = AbstractRenderer.extend({});

    return CustomReportsRenderer;
    
});